# Changelog

All notable changes are published via GitHub Releases.

- Repository: [Tideseed/eptr2](https://github.com/Tideseed/eptr2)
- Auto-generated: 2026-03-11 21:01 UTC

## Releases

### [v1.3.5 - Better Portfolio Costs and improved imbalance pricing](https://github.com/Tideseed/eptr2/releases/tag/v1.3.5) - 2026-02-03

- Tag: `v1.3.5`

This version is focused on easier portfolio cost calculations with a seriously improved portfolio cost calculation function. There is now reduced amount of information required (even fewer under some conditions), more calculations and comparisons such as "before portfolio" and "after portfolio" with advantages reported. The function is still in progress but this version is quite helpful.

See the [example file](https://github.com/Tideseed/eptr2/blob/main/examples/portfolio_costs_example.py) for a fully reproducible code snippet. 

There are several changes on the costs functions as well. Under current regulations it is not always possible to obtain correct positive and negative imbalance prices when MCP and SMP are equal. This is mainly due to system balance cannot be directly observed when MCP == SMP. So, in new functions system direction can be given as a parameter (-1 for negative imbalance, 0 for balanced and 1 for positive imbalance). If not provided the function will infer the system direction from MCP and SMP. Contradictory inputs (e.g. MCP > SMP but negative imbalance) will be overridden by the function with a warning. As a possible breaking change, ambiguous "pos" and "neg" labels are changed to "pos_imb_price" / "neg_imb_price" and "unit_pos_imb_cost" / "unit_neg_imb_cost" wherever applicable.

There are infrastructure changes as well. One significant change is now retries on timeout are at the core of the calls instead of composite functions. There are several other minor fixes and improvements as well.

### [v1.3.4 - Reorganization, Portfolio Costs Composite Function](https://github.com/Tideseed/eptr2/releases/tag/v1.3.4) - 2026-01-19

- Tag: `v1.3.4`

This version includes a set of connected composite functions around portfolio cost calculations.

+ Now with the aggregators, portfolio costs are increasingly important. `calculate_portfolio_costs` under `compose.plant_costs` handles this automatically with a given id dataframe (`id_df`) as input. Template of `id_df` can be gathered from `create_template_id_df`. Both functions have explicity export to excel functionalites.
+ You need a set of ids to get `calculate_portfolio_costs`. You can get all relevant ID lists from `get_all_important_ids` functions with excel export attribute.
+ A new cost function `calculate_diff_costs_by_contract` is added to make it easier to calculate costs.
+ There are some minor and major fixes. It is worth to mention that `calculate_unit_imbalance_price_2026` had a little bug when mcp==smp and neither of them were at the extremes.
+ Added a documentation page generated by AI. You can reach it from <https://tideseed.github.io/eptr2/>
+ Added more unit tests with new functions and functionalities

### [v1.3.3 - Cost Functions Update, Breaking Changes, Improvements and Fixes](https://github.com/Tideseed/eptr2/releases/tag/v.1.3.3) - 2026-01-09

- Tag: `v.1.3.3`

Starting from version 1.3.3 to 1.4.0 expect breaking changes as we are having an early spring cleaning on eptr2 for improved performance and compatibility with AI agents.

+ `recycle_tgt` is not `True` by default. We encourage you to use the .env approach.
+ Thanks to new recycling methodology, composite functions no longer need eptr2 object strictly as an input. 
+ Cost functions went under serious update. In order to make them compatible between distinct regulation periods (currently 2025 and 2026) automatically we added multiple wrapper functions both by indicating directly the regulation period and implied by contract. Deprecated a lot of old code to be removed by 1.4.0.  
+ Changed composite functions as well. There might be breaking changes here, too.  
+ Added tests for cost functions, composite functions.  
+ Some interim fixes and improvements.

### [v1.3.2 - Agent Skills, Cost Functions Update and New Time Function](https://github.com/Tideseed/eptr2/releases/tag/v1.3.2) - 2025-12-28

- Tag: `v1.3.2`

+ `eptr2` is following the latest AI trends and in this version we include [Agent Skills](https://code.claude.com/docs/en/skills). See [here](https://code.visualstudio.com/docs/copilot/customization/agent-skills) on how to enable them on VS Code. (Experimental)
+ Some cost function changes and name deprecations due to the finalization of relevant regulations. More information and better functions will come in the next version.
+ A new time function `contract_open_time` is added to the time functions.

### [v1.3.1 - Minor Release](https://github.com/Tideseed/eptr2/releases/tag/v1.3.1) - 2025-12-11

- Tag: `v1.3.1`

This is a minor release with some fixes.

+ Tutorials are out of the package now, because of emojis included in file names not agreeing with some systems (just to be safe).
+ Added new time function `date_str_to_contract` and added documentation to time functions.
+ TGT operations now come with timeout and improvements in TGT recycling
+ Some additional information in the README.md

### [v1.3.0 - AI Preview and ID Composite Function](https://github.com/Tideseed/eptr2/releases/tag/v1.3.0) - 2025-11-05

- Tag: `v1.3.0`

This is a major version update mainly because of the new AI capabilities of eptr2. 

+ MCP Server (fastMCP based) is added to be used with Claude Desktop, Cursor or other compatible AI.
+ Lots of documents (e.g. AGENTS.md), examples and explanations are added for LLMs and AI agents to correctly assume knowledge of the library.
+ A new composite function to get organization and UEVCB ids belonging to different periods. Also a lot of supplementary functions.

```python
## Make sure you have a .env file in the project folder with EPTR_USERNAME and EPTR_PASSWORD keys and respective values
from eptr2.composite import get_multiperiod_generation_org_and_uevcb_wrapper

res_df = get_multiperiod_generation_org_and_uevcb_wrapper(
    start_date="2025-06-01", end_date="2025-08-30", verbose=True
)
```

Alternatively 

```python
from eptr2.composite import get_multiperiod_generation_org_and_uevcb_wrapper
from eptr2 import EPTR2

eptr = EPTR2(username="EPTR_USERNAME", password="EPTR_PASSWORD")

res_df = get_multiperiod_generation_org_and_uevcb_wrapper(
    start_date="2025-06-01", end_date="2025-08-30", verbose=True, eptr=eptr
)
```

### [v1.2.4 - [CRITICAL] Authentication Method Changes](https://github.com/Tideseed/eptr2/releases/tag/v1.2.4) - 2025-10-31

- Tag: `v1.2.4`

This update is a CRITICAL update due to authentication method changes. Authentication (TGT calling) method is changed by EPIAS, so this version reflects the changes. It will be less likely to authorize (get TGT) with the previous versions.

### [v1.2.3 - Improved Authentication Method](https://github.com/Tideseed/eptr2/releases/tag/v1.2.3) - 2025-10-22

- Tag: `v1.2.3`

Improved EPTR2 object creation and and authentication with environment file (".env") (enabled by default) and TGT recycling (not enabled by default) for multi-use without carrying EPTR2 objects around.

[Details](https://github.com/Tideseed/eptr2?tab=readme-ov-file#alternative-using-env-file-for-credentials-and-tgt-recycling) of this new authentication method.

### [v1.2.2 - Bulk Calls](https://github.com/Tideseed/eptr2/releases/tag/v1.2.2) - 2025-10-13

- Tag: `v1.2.2`

Main development of this version is the addition of the new bulk calls from EPIAS Transparency Platform. Simply get UEVÇB ids from organization ids, get real-time generation and dpp (kgüp) in bulk.

```python
### DPP / KGÜP bulk
dpp_df = eptr.call("dpp-bulk", date="2025-08-31", uevcb_ids=[3208611, 723724, 335505, 106710, 4094])
### Real Time Generation bulk
rt_gen_df = eptr.call("rt-gen-bulk", date="2025-08-31", pp_ids=[11162, 17889, 605, 5005121, 20764])
### UEVCB list bulk
uevcb_df = eptr.call("uevcb-list-bulk", date="2025-08-31", org_ids=[166, 12297, 648, 19880, 162])
```

New time functions are added. Given a contract name (e.g. `contract = "PH25101321"`), you can now get `n` previous contracts with `get_previous_contracts(c=contract,n=5)`, next contracts with `get_next_contracts(c=contract,n=5)` and both with `get_contract_range_from_center(c=contract, n_before=2, n_after=2)`

Also there are some minor fixes and improvements including the `.gitignore` incident at the previous version.

### [v1.2.1 - Draft Imbalance Costs for 2026](https://github.com/Tideseed/eptr2/releases/tag/v1.2.1) - 2025-09-29

- Tag: `v1.2.1`

This version includes some minor improvements and example calculation function for proposed 2026 imbalance costs.

### [v1.2.0 - Plant Costs](https://github.com/Tideseed/eptr2/releases/tag/v1.2.0) - 2025-09-15

- Tag: `v1.2.0`

This is a loaded release

+ New composite function: **Plant Costs**. Until recently, in order to evaluate a plant's performance, you needed to get the KGÜP/KUDÜP from one source, real time generation or UEVM from another source and costs data (MCP/PTF, SMP/SMF) from another source. This is no longer the case with the new plant costs function. Details [here](https://github.com/Tideseed/eptr2/tree/v1.2.0?tab=readme-ov-file#plant-costs). (ps. You are limited to transparency data so some generation values of balancing or auxiliary market might not be provided)
+ Plant costs function tutorial. There is a new interface at the tutorials (eptr2demo). Still in beta phase though, expect changes.
+ Fixes, improvements and additons to time and cost functions.
+ Switch to uv as the package manager
+ Minor fixes and improvements

<img width="995" height="1200" alt="Screenshot 2025-09-15 at 16 57 02" src="https://github.com/user-attachments/assets/138cec95-d296-40a9-8fe9-4a0af4902802" />
<img width="994" height="1189" alt="Screenshot 2025-09-15 at 16 57 08" src="https://github.com/user-attachments/assets/87d3a59b-5d6b-4fb9-a1ac-30f42690b0f6" />
<img width="973" height="921" alt="Screenshot 2025-09-15 at 16 57 16" src="https://github.com/user-attachments/assets/0201aefb-0a98-44d1-b961-90d61f552e65" />
<img width="979" height="930" alt="Screenshot 2025-09-15 at 16 57 19" src="https://github.com/user-attachments/assets/b977556c-b327-41bb-b277-44c1fdf6ad3d" />

### [v1.1.4 - Price and Cost Composite Function Tutorial](https://github.com/Tideseed/eptr2/releases/tag/v1.1.4) - 2025-09-04

- Tag: `v1.1.4`

This release includes the demo of a new composite function to the demo: Price and Cost. You can get MCP (PTF), SMP (SMF), WAP (AOF) and other data (imbalance prices/costs, KUPST) with a single function. Here are the other updates.

+ You can run demo UI by simply writing `eptr2demo` to the CLI (terminal).
+ Fixed production plan tutorial bugs related to start-end time selections.
+ Added a new time function `contract_duration` which would return the total lifetime of a contract in seconds, minutes or hours.
+ Fixed a bug in settlement_date calculations.
+ Updated README.md

<img width="1010" height="999" alt="Screenshot 2025-09-04 at 11 05 54" src="https://github.com/user-attachments/assets/c13660c5-8fe9-4930-819a-ac7e0aa36346" />

### [v.1.1.3 - Production Plan Composite Function Tutorial](https://github.com/Tideseed/eptr2/releases/tag/v1.1.3) - 2025-08-27

- Tag: `v1.1.3`

Added production plan (KGÜP-KUDÜP) usage page to main tutorial.

<img width="999" height="802" alt="Screenshot 2025-08-27 at 22 55 53" src="https://github.com/user-attachments/assets/21eb825d-c7e3-4ec1-9e81-57a0fa8c2dbd" />


Also, you can get the example code dynamically on the same page.

<img width="951" height="545" alt="Screenshot 2025-08-27 at 22 56 01" src="https://github.com/user-attachments/assets/8407e116-057e-42e7-bfd8-82aaec0bbbc7" />

### [v1.1.2 - Composite Function Tutorials](https://github.com/Tideseed/eptr2/releases/tag/v1.1.2) - 2025-08-08

- Tag: `v1.1.2`

**Changelog**

+ Composite Functions tutorial (Streamlit): Organization Positions
+ Minor fixes and improvements

---

This version is the best of both worlds: eptr2's composite functions and streamlit tutorials. If you are registered to EPIAS Transparency Platform (currently free) you can use a functioning Streamlit app directly in your computer. Just run the following code after you update to version 1.1.2. With following versions, more composite functions will be added to this UI. You will also get a "cheat sheet" at the bottom to run the python code.

```python
from eptr2.tutorials import run_composite_app

run_composite_app(
    username="YOUR_TRANSPARENCY_USERNAME",
    password="YOUR_TRANSPARENCY_PASSWORD",
)
```

## UI Example

_p.s. The UI is Turkish but can easily be translated in browser._

<img width="1333" height="1180" alt="Screenshot 2025-08-08 at 11 19 27" src="https://github.com/user-attachments/assets/e244acbc-d6bd-4530-a5e8-d0231b7fe14c" />

### [v1.1.1 - Day Ahead with IDM Composite](https://github.com/Tideseed/eptr2/releases/tag/v1.1.1) - 2025-07-20

- Tag: `v1.1.1`

Now you will be able to gather the positions of an organization with a single composite command `get_dabi_idm_data`. If you just want to get the data for a period (month), then you can use the `get_dabi_idm_data_period` wrapper.

```python
from eptr2 import eptr_w_tgt_wrapper
from eptr2.composite import get_dabi_idm_data, get_dabi_idm_data_period

df = get_dabi_idm_data(
    eptr,
    start_date="2025-06-01",
    end_date="2025-06-30",
    org_id="195", ## EÜAŞ
    verbose=True,
)

df = get_dabi_idm_data_period(
    eptr,
    period="2025-06-01",
    org_id="195",
    verbose=True,
)

## To get organizations info with ids
org_df = eptr.call("dam-clearing-org-list", period="2025-06-01")
```

In addition there are some fixes and improvements especially in costs and time functions.

### [v1.1.0 - Credentials Management](https://github.com/Tideseed/eptr2/releases/tag/v1.1.0) - 2025-07-12

- Tag: `v1.1.0`

This version is about advanced credentials management. You can select a JSON file with fields `EPTR_USERNAME` and `EPTR_PASSWORD` and run the new wrapper function with the `creds_path` parameter (default value is `"creds/eptr_credentials.json"`). 

```python
from eptr2 import eptr_w_tgt_wrapper

eptr = eptr_w_tgt_wrapper()
```

If you want to generate the credentials file first you can use the following helper function.

```python
generate_eptr2_credentials_file(
    username = "YOUR_USERNAME",
    password = "YOUR_PASSWORD",
    cred_path = "creds/eptr_credentials.json",
    overwrite: bool = False,
)
```

In addition we have minor fixes, improvements and documentation updates.

### [v1.0.8 - BPM Composite and Dependency Update](https://github.com/Tideseed/eptr2/releases/tag/v1.0.8) - 2025-06-25

- Tag: `v1.0.8`

+ Now you can get BPM (Weighted Average) data in bulk from EPIAS Transparency Platform (see README for examples).
+ `urllib3` dependency is bumped up to 2.5.0 due to Github security warnings

### [v1.0.7 - Day Ahead and Bilateral Composite Function](https://github.com/Tideseed/eptr2/releases/tag/v1.0.7) - 2025-06-13

- Tag: `v1.0.7`

This version includes the introduction of day ahead and bilateral agreements composite function. With a single function get both DA and BI data and join them seamlessly. Check `get_day_ahead_and_bilateral_matches` and `get_day_ahead_detail_info` functions under `composite/dabi.py`.

Additionally some dependency updates due to critical vulnerability warnings. Probably nothing too alarming but still such effort is good practice.

### [v1.0.6 - IDM Log Composite](https://github.com/Tideseed/eptr2/releases/tag/v1.0.6) - 2025-05-26

- Tag: `v1.0.6`

In this version, a wrapper to overcome the limits of IDM log is introduced. 

IDM log can only be called for weekly periods, but occasionally we need to obtain longer periods (e.g. a month). `idm_log_longer` gets a start and an end date (`start_date`, `end_date`) and partitions the calls in weekly periods. 

If `contract_wise` is `True` the function act consciously and gets data from previous day of the start date as well.

As a wrapper to get the whole months, it is possible to use `idm_log_period` by just stating the month date as period (e.g. 2025-05-01). 

You can find the functions under the composite module.

```python
from eptr2.composite.idm_log import idm_log_longer, idm_log_period
```

Plus, minor revisions and fixes.

### [v1.0.5 - Time Functions](https://github.com/Tideseed/eptr2/releases/tag/v1.0.5) - 2025-04-12

- Tag: `v1.0.5`

+ New time functions such as floor/ceiling prices, checks for settlement, time to contract close etc.
+ Fixed some README instructions
+ Minor Improvements

### [v1.0.4 - MMS Composite and Improvements](https://github.com/Tideseed/eptr2/releases/tag/v1.0.4) - 2025-03-25

- Tag: `v1.0.4`

This version's major developments are

- Option to include hourly contract names to composite functions with `include_contract_symbol = True` (default: False) for better IDM analysis.
- New composite function that gets Market Message System data, flattens it and calculates energy and power losses per contract.

In addition,

- Tests are added for composite functions
- A temporary fix is added for Imbalance Quantity (Dengesizlik Miktarı) because it somehow does not return any value if start_date is not start of month.
- Minor fixes and improvements

### [v1.0.3 - Minor Updates](https://github.com/Tideseed/eptr2/releases/tag/v1.0.3) - 2025-02-25

- Tag: `v1.0.3`

+ Improved cost calculation function `calculate_diff_cost`: With a single function you can calculate imbalance costs, KÜPST with breakdowns and all other details.

```python
from eptr2.util.costs import calculate_diff_cost

res = calculate_diff_cost(
    actual=50, forecast=100, mcp=1000.0, smp=2500.0, prod_source="solar"
)

## print(res)
# {
#     "imbalances": {
#         "diff": -50,
#         "dsg_imbalance": -5.0,
#         "net_dsg_imbalance": -5.0,
#         "individual_imbalance": -45.0,
#         "imb_tol_value": 5.0,
#     },
#     "imb_side": "neg",
#     "costs": {
#         "ind_imbalance_cost": 70875.0,
#         "dsg_imb_cost": 7875.0,
#         "net_dsg_imb_cost": 7875.0,
#         "total_imb_cost": 78750.0,
#     },
#     "kupst": {
#         "kupst_tol_perc": 0.1,
#         "kupsm_tol": 10.0,
#         "kupsm": 40.0,
#         "unit_kupst_cost": 75.0,
#         "kupst_cost": 3000.0,
#     },
# }

```

+ Fixed a parameter regulating TGT update times
+ Added contract indicators to some composite functions

### [v1.0.2 - Production Composite and New Tutorial](https://github.com/Tideseed/eptr2/releases/tag/v1.0.2) - 2025-01-29

- Tag: `v1.0.2`

Breaking changes in some functions in `util.costs`. Added `_deprecated` suffix to deprecated functions, they will be removed from v1.1.0. 

New composite functions to gather production data

+ `get_hourly_production_data`
+ `get_hourly_production_plan_data`
+ `wrapper_hourly_production_plan_and_realized`

New streamlit tutorial to calculate imbalance and KÜPST costs 
<img width="1492" alt="Screenshot 2025-01-29 at 16 46 07" src="https://github.com/user-attachments/assets/de436263-d61e-4ac7-b074-1b898a7fb4cf" />

```python
from eptr2.tutorials import run_calc_app

run_calc_app(username="YOUR_USERNAME",password="YOUR_PASSWORD")
```

### [v1.0.1 - Price and Cost](https://github.com/Tideseed/eptr2/releases/tag/v1.0.1) - 2025-01-04

- Tag: `v1.0.1`

+ Two new composite functions: `get_price_and_costs`, `get_imbalance_data`
+ Minor fix in composite function `get_hourly_consumption_and_forecast_data`. End date was mistakenly filled with start date in demand forecast.

### [v1.0.0 - A New Era](https://github.com/Tideseed/eptr2/releases/tag/v1.0.0) - 2024-12-23

- Tag: `v1.0.0`

Finally, after more than a year, we have the v1.0.0. Though v1.0.0 is not an end, but a beginning. We had the complete call coverage at v0.7.3. With v1.0.0+, eptr2 will provide opportunities to actually use Transparency in your reports and dashboards. Therefore v1.0.0 is not an end but a beginning.

We have two major developments.

## Tutorial Interfaces

We used to refer people to [eptr2demo](https://eptr2demo.streamlit.app/) to figure out which calls to make and how to configure them. Now you can call it inside the package. 

```python
from eptr2.tutorials import run_demo_app
run_demo_app(username="YOUR_USERNAME",password="YOUR_PASSWORD")
```

_Tutorial interfaces are currently experimental and they may include breaking changes._

## Composite Functions

Each call in eptr2 gets a different data set. But sometimes we need several sets of data together to have the full picture of a time period. We bring in composite functions in this version. 

We start with an example. Consumption data is important for many analysts. There is real time consumption, settlement consumption (UECM) and load plan (forecasted consumption). Our first composite function `get_hourly_consumption_and_forecast_data` brings all three data sources together.

```python
from eptr2 import EPTR2
from eptr2.composite import get_hourly_consumption_and_forecast_data

eptr = EPTR2(username="YOUR_USERNAME",password="YOUR_PASSWORD")
df = get_hourly_consumption_and_forecast_data(eptr, start_date="2024-07-29", end_date="2024-07-29")
print(df)
```

If you have good ideas about composite functions, please write to us. 

_Composite functions are currently experimental and they may include breaking changes._

## Other changes

We are improving the `eptr2` experience continuously. Now, it is possible to register username and password from a file or through environment variables. A credentials json file shall look like this one.

```json
{
    "EPTR_USERNAME": "YOUR_TRANSPARENCY_USER_EMAIL",
    "EPTR_PASSWORD": "YOUR_TRANSPARENCY_PASSWORD"
}
```

Just add the extra parameter (`credentials_file_path`) and json file path to the `EPTR2` object.

Alternatively you can define environment variables "EPTR_USERNAME" and "EPTR_PASSWORD" and EPTR2 object will take care of those as credentials as well.

### [v0.7.3 - Penultimate Release](https://github.com/Tideseed/eptr2/releases/tag/v0.7.3) - 2024-12-19

- Tag: `v0.7.3`

This is the last release before v1.0.0 🎉

+ Almost complete command set (excluding some admin commands and commands not functioning since 2016). A total of 213 commands with help documents.
+ Default value of minimum 750 TL/MWh in KUPST calculations in line with the new legislation.
+ Minor fixes and improvements

### [v0.7.2 - VEP](https://github.com/Tideseed/eptr2/releases/tag/v0.7.2) - 2024-12-15

- Tag: `v0.7.2`

+ Added VEP / PFM Calls
+ Added `min_kupst` parameter for preparation for minimum unit KUPST price (default: 0 for now)
+ Added `kgup-v1` call to get KGÜP / FDPP first version

### [v0.7.1 - YEK-G](https://github.com/Tideseed/eptr2/releases/tag/v0.7.1) - 2024-11-28

- Tag: `v0.7.1`

+ Included YEK-G calls
+ Background changes
+ New method (`get_number_of_calls`) to get the number of calls.
+ A total of 186 original API calls, in addition some derived calls with specific parameter combinations.

### [v0.7.0 - Aliases](https://github.com/Tideseed/eptr2/releases/tag/v0.7.0) - 2024-11-20

- Tag: `v0.7.0`

+ There are now aliases that can point to calls. For instance, "ptf" also points to "mcp" call. More aliases to come in the future.
+ `custom_aliases` parameter is introduced (see Readme for detailed explanation) so that you can define your own aliases.
+ Some NG-VGP calls had either period and start-end date parameter set distinctions. Wrapper functions are built for the calls.

### [v0.6.2](https://github.com/Tideseed/eptr2/releases/tag/v0.6.2) - 2024-11-13

- Tag: `v0.6.2`

+ New natural gas calls. Natural gas calls are completely covered.
+ Totaling up to 175 calls.
+ Some minor fixes and improvements.

### [v0.6.1 - Natural Gas Calls Part 2](https://github.com/Tideseed/eptr2/releases/tag/v0.6.1) - 2024-10-29

- Tag: `v0.6.1`

+ Added 15 more natural gas calls. Some more to go.
+ Some simplifications for the path mapping (background improvements, insignificant at runs)

### [v0.6.0 - Natural Gas Calls](https://github.com/Tideseed/eptr2/releases/tag/v0.6.0) - 2024-10-06

- Tag: `v0.6.0`

This version adds new natural gas calls. You can see the list of calls beginning with "ng-" prefix with the command `EPTR2().get_available_calls()`.

### [v0.5.5 - Minor Fixes and New Calls (RA/GDDK)](https://github.com/Tideseed/eptr2/releases/tag/v0.5.5) - 2024-09-24

- Tag: `v0.5.5`

Updating to this version is recommended if you have long running (>8 hrs) transparency processes.

- Minor bug to prevent automatic TGT check is fixed
- A deprecation warning is converted to yield error
- 5 new calls for retrospective adjustment (GDDK) are added

### [v0.5.4 - Datetime bug fix for some versions](https://github.com/Tideseed/eptr2/releases/tag/v0.5.4) - 2024-09-01

- Tag: `v0.5.4`

datetime package of some Python versions had a problem with `fromisoformat` method when parsing TGT start time. With this fix start time defaults to `now` if any error occurs.

### [v0.5.3 - Mini Update](https://github.com/Tideseed/eptr2/releases/tag/v0.5.3) - 2024-08-27

- Tag: `v0.5.3`

With this version you will be able to skip the credentials (username and password) with the `skip_credentials=True` parameter when initializing `EPTR2` object. 

This update is due to the possibility that EPIAS would postpone authentication requirements for the Transparency Platform. Though remember EPIAS requires now and will require registration in the future. Therefore registration to the platform and using username/password is highly recommended.

### [v0.5.2 - Pre-live Update](https://github.com/Tideseed/eptr2/releases/tag/v0.5.2) - 2024-08-14

- Tag: `v0.5.2`

This update makes the necessary changes for the live Transparency Platform logins. 

+ You should go to <https://giris.epias.com.tr/> and register with an email and password.
+ You should add `username` and `password` to your `EPTR2` object initialization. (No need to add `is_test=False`, default value is False)
+ The object handles TGT generation and updates automatically with soft and hard expiration times.
+ Optional: If you want to use your TGT in further iterations, you can get use the method `export_tgt_info` and then add the dictionary to EPTR2 object as a parameter `tgt_d`.

### [v0.5.1](https://github.com/Tideseed/eptr2/releases/tag/v0.5.1) - 2024-07-31

- Tag: `v0.5.1`

+ A fix is applied to path formation that is preventing convenience function
+ Soft timeout for TGT is applied (1.5 hours - EPIAS soft limit is 2 hr)
+ Periodic check for TGT timeout is added to calls

ps. These changes are for the test server. No changes on the live server until August 19.

### [v0.5.0 - Credentials Update](https://github.com/Tideseed/eptr2/releases/tag/v0.5.0) - 2024-07-31

- Tag: `v0.5.0`

On [July 30](https://seffaflik.epias.com.tr/announcements/announcements), EPIAS announced that they will start to use credentials on API calls soon. This version makes `eptr2` compatible for the new methodology.

- New parameters `username`, `password` and `is_test` are accepted in the initialization of `EPTR2` object. `is_test` parameter default value is `False`, others are `None` (ps. username and password will be required in the following versions).
- `TGT` is generated automatically in the object. You can renew TGT using `check_renew_tgt` method. Even though technical documents indicate an 8 hour duration, expiration is set to 6 hours. 

Note that these changes are currently only applicable to test server. You need to register from [here](https://giris-prp.epias.com.tr/).

According to the announcement, changes will be taken to live on August 19, 2024.

### [v0.4.2 - Minor Changes](https://github.com/Tideseed/eptr2/releases/tag/v0.4.2) - 2024-06-06

- Tag: `v0.4.2`

- Fixes and improvements
- Added services

### [v0.4.1 - Minor Update](https://github.com/Tideseed/eptr2/releases/tag/v0.4.1) - 2024-04-17

- Tag: `v0.4.1`

This is an interim release to fix minor points.

+ `pandas` is no longer required in time related utility functions (`util.time`) 
+ `root_phrase` can be set in `EPTR2` object

### [v0.4.0 - Reduced Dependencies](https://github.com/Tideseed/eptr2/releases/tag/v0.4.0) - 2024-04-16

- Tag: `v0.4.0`

This release can be considered a major release in terms of dependency simplification and making it lighter for the use of other applications.

+ `requests` library is replaced with `urllib3` library
+ `pandas` is now optional. If you do not provide `postprocess=True`, when initiating your `EPTR2` object you will directly get the response dictionary
+ `get_raw_response` is a new parameter (default: false) if you want the raw `urllib3` response.

### [v0.3.0 - 100 services](https://github.com/Tideseed/eptr2/releases/tag/v0.3.0) - 2024-04-01

- Tag: `v0.3.0`

+ 100 services covered!
+ "Ancillary services" (Yan Hizmetler) calls are added to convenience calls
+ "Number of licenses per participant type" call is added to convenience calls
+ Python version requirement is reduced to 3.9.6
+ Steps taken to reduce dependencies
+ More calls are added to help information

### [v0.2.3 - Minor Update](https://github.com/Tideseed/eptr2/releases/tag/v0.2.3) - 2024-03-21

- Tag: `v0.2.3`

- Minor fixes to some functions
- Cost calculation functions update (still in beta)

### [v0.2.2](https://github.com/Tideseed/eptr2/releases/tag/v0.2.2) - 2024-02-23

- Tag: `v0.2.2`

- Fixed a problem with supply-demand call
- Changed parameters for bpm-orders and bpm-orders-w-avg from `date_time`to `date`. A deprecation warning is issued. Support will be removed from v0.3.0.
- Some internal fixes

### [v0.2.1](https://github.com/Tideseed/eptr2/releases/tag/v0.2.1) - 2024-02-16

- Tag: `v0.2.1`

- New calls (mostly from electricity reporting)
- Security update: `ssl_verify` is now True as default
- A new date format `se_date` is introduced. Some services require start date and end date as the same date. Therefore start/end date is introduced as a single date to regulate both start date and end date.
- A few internal changes for better parameter processing

### [v0.2.0](https://github.com/Tideseed/eptr2/releases/tag/v0.2.0) - 2024-01-31

- Tag: `v0.2.0`

- Dam related calls
- Minor enhancements

### [v0.1.9](https://github.com/Tideseed/eptr2/releases/tag/v0.1.9) - 2024-01-10

- Tag: `v0.1.9`

- Bug-fix on UEVM power plant id mapping
- Basic imbalance and KÜPST calculations (to be improved)

### [v0.1.8](https://github.com/Tideseed/eptr2/releases/tag/v0.1.8) - 2023-12-20

- Tag: `v0.1.8`

Skipped a few interim versions. Here is v0.1.8.

+ Help part now covers all current calls (check https://eptr2demo.streamlit.app/)
+ A few new calls (now 87 calls are covered)

### [v0.1.5](https://github.com/Tideseed/eptr2/releases/tag/v0.1.5) - 2023-12-12

- Tag: `v0.1.5`

+ More services (Transmission Part 2, Market Message System (Piyasa Mesaj Sistemi))

### [v0.1.4](https://github.com/Tideseed/eptr2/releases/tag/v0.1.4) - 2023-12-11

- Tag: `v0.1.4`

+ More services are added from generation, consumption, transmission and renewables (YEKDEM) categories. 
+ Total number of services are 70

### [v0.1.3](https://github.com/Tideseed/eptr2/releases/tag/v0.1.3) - 2023-12-08

- Tag: `v0.1.3`

- Calls and convenience calls are updated for smoother data ingestion
- New convenience calls are added. Currently covering 41 (out of 186) API calls. New calls cover Day Ahead (GÖP), Intraday (GİP), Balancing (DGP, Dengesizlik), Bilateral contracts (İkili Anlaşmalar), and some listings.

